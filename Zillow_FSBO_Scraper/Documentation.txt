# Zillow Scraper Documentation

## Prerequisites
1.  **Python 3.10+**: Ensure you have a modern version of Python installed.
    *   Download: https://www.python.org/downloads/
2.  **Zyte API Key**: You need a valid Zyte API key.

## Installation

1.  **Open Terminal**: Navigate to the project folder.
    ```bash
    cd path/to/zillow_scraper
    ```

2.  **Create Virtual Environment** (Recommended):
    ```bash
    python -m venv venv
    ```

3.  **Activate Virtual Environment**:
    *   **Windows**:
        ```bash
        venv\Scripts\activate
        ```
    *   **Mac/Linux**:
        ```bash
        source venv/bin/activate
        ```

4.  **Install Dependencies**:
    ```bash
    pip install -r requirements.txt
    ```

## Configuration

1.  **API Key**:
    *   The project uses a `.env` file to store your API key securely.
    *   Ensure the `.env` file exists in the root directory and contains:
        ```
        ZYTE_API_KEY=your_actual_api_key_here
        ```
    *   (Note: I have already created this file for you with your current key).

2.  **Input Locations**:
    *   Edit `input/locations.csv` to add or remove locations.
    *   Format: `location` (header), followed by rows like `miami dade county fl`.

## Running the Scraper

### Windows
*   Double-click `runner.bat`.

### Command Line (Windows/Mac/Linux)
Make sure your virtual environment is activated, then run:
```bash
scrapy crawl zillow_spider
```

## Output
*   Results are saved in the `outputs/` folder with a timestamped CSV file (e.g., `zillow_24_Nov_2025_....csv`).
